name: ci

on:
  push:
    branches:
    - main
    - develop
  pull_request:
    branches:
    - main
    - develop

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Setup Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install -e .
        python -m pip install pytest pytest-cov mkdocs mkdocs-material "mkdocstrings[python]"

    - name: Run tests
      run: python -m pytest --cov=stock_prices --cov-report=term-missing

    - name: Build documentation
      run: python -m mkdocs build --strict

    - name: Compile Python sources
      run: python -m compileall -q src tests scripts

    - name: Build Docker image
      run: |
        cp .env.example .env
        docker compose config --quiet
        docker build -t stock-prices:ci .
        docker run --rm stock-prices:ci python -m stock_prices --debug-info
